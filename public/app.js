import { installBrowserWebMCP } from '/webmcp.js';
const $=(id)=>document.getElementById(id);const traceEl=$('trace');const resultEl=$('result');
function addTrace(event){const empty=traceEl.querySelector('.empty');if(empty)empty.remove();const row=document.createElement('div');row.className=`event ${event.state||'info'}`;row.innerHTML=`<time>${new Date().toLocaleTimeString()}</time><div><strong>${event.title}</strong><p>${event.detail||''}</p></div>`;traceEl.prepend(row);}
async function invoke(name,input={}){addTrace({title:`WebMCP → ${name}`,detail:JSON.stringify(input),state:'info'});const response=await fetch(`/api/tools/${encodeURIComponent(name)}`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(input)});const data=await response.json();addTrace({title:`${name} · ${data.state||(data.ok?'ok':'error')}`,detail:data.blocker||data.error||'Completed',state:data.ok?'ok':(data.state||'blocked')});return data;}
async function boot(){try{const health=await fetch('/api/health').then(r=>r.json());$('health').textContent=health.ok?'Bridge: online':'Bridge: unavailable';const policy=await fetch('/api/policy').then(r=>r.json());$('agents').innerHTML=policy.agents.map(agent=>`<article><span class="dot"></span><div><strong>${agent}</strong><small>configured · state unknown until queried</small></div></article>`).join('');const registration=installBrowserWebMCP(invoke);$('mcpBadge').textContent=registration.supported?`${registration.registered.length} WebMCP tools registered`:'Browser WebMCP unavailable';$('mcpBadge').classList.toggle('ok',registration.supported);addTrace({title:'Console initialized',detail:registration.supported?'WebMCP tool registration succeeded.':'Standard browser mode. WebMCP API not present.',state:registration.supported?'ok':'info'});}catch(error){$('health').textContent='Bridge: unavailable';addTrace({title:'Boot failed',detail:error.message,state:'blocked'});}}
$('dispatchForm').addEventListener('submit',async(event)=>{event.preventDefault();const input={agent:$('agent').value,project:$('project').value,instruction:$('instruction').value};const data=await invoke('dispatch_agent_action',input);resultEl.textContent=JSON.stringify(data,null,2);});$('clearTrace').addEventListener('click',()=>{traceEl.innerHTML='<div class="empty">No execution events yet.</div>';});boot();
